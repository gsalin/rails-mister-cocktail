




  <h1> <%= @cocktail.name %></h1>

  <% @cocktail.doses.each do |dose| %>
    <p> <%= dose.description %> : <%= dose.ingredient.name %> - <%= link_to "effacer cet ingredient", dose_path(dose), method: :delete %> </p>
  <% end %>
<ul>
  <li> <%= link_to "Ajouter un ingredient", new_cocktail_dose_path(@cocktail) %> </li>
  <li> <%= link_to "Retourner à la liste des cocktails", cocktails_path %> </li>
</ul>


<% if @cocktail.photo? %>
  <%= cl_image_tag @cocktail.photo.path, width: 200, height: 300, crop: :fill %>
<% end %>

